#include <stdio.h>

int main(void)
{
    int x;
    int y;

    printf("Please enter interger for X followed by one for Y (e.i., 2 6)\n");
    scanf("%d %d", &x, &y);

    //if (y < x)
    //if (y <= x)
    //if (y > x)
    //if (y >= x)
    if (y == x)
    //if (y != x)
    //if (2 == x)
    //if (6 != x)
    //if (6 >= 2)
    //if (2 < 6)
    //if (x != y != 3 >= x)

    {
    printf("TRUE or 1 \n");
    }

    else (printf("FALSE or 0 \n"));

    return 0;

}